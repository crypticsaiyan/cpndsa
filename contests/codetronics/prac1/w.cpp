#include <bits/stdc++.h>
using namespace std;

// string str; getline(cin, str);

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int t, m, n;
    cin >> t;
    for (int i = 0; i < t; i++)
    {
        cin >> n >> m;
        int arr[n][m];
        for (int j = 0; j < n; j++)
        {
            for (int k = 0; k < m; k++)
            {
                cin >> arr[j][k];
            }
        }
        vector<int> v;
        // for (int j = 0; j < m; j++)
        // {
        //     v.push_back(arr[])
        // }
        
    }

    return 0;
}